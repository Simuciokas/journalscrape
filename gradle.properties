org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# 26.2 has no yarn build and an empty intermediary table, so the jar's own official names are the
# only mapping there is - see build.gradle.
minecraft_version=26.2
loader_version=0.19.5
mixin_version=0.17.1+mixin.0.8.7

mod_version=1.3.0
maven_group=io.github.simuciokas
archives_base_name=journalscrape

# Needs a JDK 25 toolchain (26.2 is class-file major 69); set JAVA_HOME or list one here:
#org.gradle.java.installations.paths=/path/to/jdk-25
